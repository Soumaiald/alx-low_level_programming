#include "main.h"
#include <ctype.h>

/**
 * isupper - returns 1 if true else returns 0
 * @c: char
 * Return: 1 or 0
 */

int _isupper(char c)
{
	if(isupper(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
